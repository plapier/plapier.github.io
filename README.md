# Start jekyll
grunt serve

# Compile Coffee & Sass
grunt compile

-- Archive --

Run Jekyll
jekyll serve -w

Install and run coffeebar for compressed output
coffeebar -m -w js/

Run Sass
sass --watch --style compressed stylesheets
